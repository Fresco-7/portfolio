/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "img.etimg.com",
            "assets.vogue.com",
            "m.media-amazon.com",
            "upload.wikimedia.org",
            "aceternity.com",
        ],
    },

};

export default nextConfig;
